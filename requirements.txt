# ── RL / environment ─────────────────────────────────────────────────────────
gymnasium>=0.29,<1.0
minigrid>=2.3,<3.0

# ── deep learning (DNN/Linear baselines, GPU-accelerated tensor ops) ─────────
torch>=2.2

# ── numerics / ML ─────────────────────────────────────────────────────────────
numpy
scipy
scikit-learn

# ── config / logging ─────────────────────────────────────────────────────────
pyyaml
tqdm
rich
pandas
wandb          # optional: only used if a config sets use_wandb: true
